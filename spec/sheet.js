"use strict";

describe("Sheet", function () {

  /*
  it("defined", function () {
    expect(oak.canvas.Sheet).toBeDefined();
  });

  describe("properly sets crop dimensions", function () {
    var img;
    beforeEach(function () {
      oak.canvas.renderer.pixelRatio = 1;
      img = new oak.canvas.Sheet({cropX: 20, cropY: 30, width: 100, height: 100});
    });

    it("defines dimention props", function () {
      expect(img.cropX).toBeDefined();
      expect(img.cropY).toBeDefined();
      expect(img.cropWidth).toBeDefined();
      expect(img.cropHeight).toBeDefined();
    });

    it("sets non-retina crop dimensions", function () {
      expect(img.cropX).toBe(20);
      expect(img.cropY).toBe(30);
      expect(img.cropWidth).toBe(100);
      expect(img.cropHeight).toBe(100);
    });
    
    it("sets retina crop dimensions", function () {
      oak.canvas.renderer.pixelRatio = 2;
      img = new oak.canvas.Sheet({cropX: 20, cropY: 30, width: 100, height: 100});

      expect(img.cropX).toBe(40);
      expect(img.cropY).toBe(60);
      expect(img.cropWidth).toBe(200);
      expect(img.cropHeight).toBe(200);
    });

  });

  describe("shifts", function () {
    var img;
    beforeEach(function () {
      oak.canvas.renderer.pixelRatio = 1;
      img = new oak.canvas.Sheet({});
    });
    
    afterEach(function () {
    });

    it("shifts", function () {
      expect(img.shift).toBeDefined();

      img.shift(10, 10);

      expect(img.cropX).toBe(10);
      expect(img.cropY).toBe(10);
    });

    it("shifts for retina", function () {
      expect(img.shift).toBeDefined();

      oak.canvas.renderer.pixelRatio = 2;
      img.shift(10, 10);

      expect(img.cropX).toBe(20);
      expect(img.cropY).toBe(20);
    });

  });
  */

});
